{
  "name": "portfolio-site-hyperapp",
  "version": "1.0.0",
  "description": "Portfolio Site in Hyperapp for starters",
  "main": "index.js",
  "scripts": {
    "build": "./build/index.sh",
    "build-css": "node-sass-chokidar src/ -o src/",
    "watch-css": "npm run build-css && node-sass-chokidar src/ -o src/ --watch --recursive",
    "deploy": "",
    "post-build": "rm -rf ./dist/*.map"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "live-server": "^1.2.1",
    "mustache": "^3.1.0",
    "onchange": "^6.1.0",
    "showdown": "^1.9.0",
    "uglify-js": "^3.6.5"
  }
}
