{
  "name": "grifstuf",
  "version": "1.0.0",
  "description": "grifstuf",
  "main": "./dist/index.html",
  "scripts": {
    "setup": "rm -rf ./dist && cp -R ./public ./dist && mkdir ./dist/blog",
    "build": "npm run setup && node ./build/index.js",
    "build-css": "node-sass-chokidar src/ -o src/",
    "watch-css": "npm run build-css && node-sass-chokidar src/ -o src/ --watch --recursive",
    "deploy": "",
    "post-build": "rm -rf ./dist/*.map"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "live-server": "^1.2.1",
    "mustache": "^3.1.0",
    "onchange": "^6.1.0",
    "showdown": "^1.9.0",
    "uglify-js": "^3.6.5"
  }
}
